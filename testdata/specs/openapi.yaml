openapi: 3.0.3
info:
  title: QA demo
  version: 1.0.0
paths:
  /anything:
    post:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  json:
                    type: object
                    properties:
                      echo:
                        type: string
                required: [json]
