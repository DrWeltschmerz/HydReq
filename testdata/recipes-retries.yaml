name: recipes - retries
baseUrl: ${ENV:HTTPBIN_BASE_URL}
vars:
  HTTPBIN_BASE_URL: https://httpbin.org
tests:
  - name: GET retried
    request: { method: GET, url: /status/200 }
    assert: { status: 200 }
    retry: { max: 3, backoffMs: 200, jitterPct: 25 }
